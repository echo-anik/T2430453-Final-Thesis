# Core ML Libraries
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0

# PyTorch - OPTIONAL (install with: pip install -e ".[gpu]" or pip install -e ".[cpu]")
# For GPU (NVIDIA RTX 3060 Ti / 4070 Ti):
#   pip install -e ".[gpu]"
# For CPU only:
#   pip install -e ".[cpu]"
# For development:
#   pip install -e ".[dev]"


# Conditional PyTorch (uncomment ONE of these)
# torch>=2.0.0; extra == "gpu"
# torchvision>=0.15.0; extra == "gpu"
# torchaudio>=2.0.0; extra == "gpu"
# torch-cpu>=2.0.0; extra == "cpu"

# For ONNX export (Jetson Nano deployment)
onnx>=1.12.0; extra == "gpu"
onnxruntime>=1.12.0; extra == "gpu"

# Visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Utilities
tqdm>=4.62.0
PyYAML>=6.0
